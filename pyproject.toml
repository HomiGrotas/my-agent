[project]
name = "my-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "homigrotas", email = "homigrotas2020@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai>=3.11.0",
]

[project.scripts]
my-agent = "my_agent:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
